{
  "name": "spfx-relay",
  "version": "0.1.0",
  "description": "Migrates legacy SharePoint Script Editor customizations (HTML/CSS/jQuery) into compiling, linted SPFx React web parts — a deterministic pipeline with LLM-powered transforms, hard verification gates, and honest refusals.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "license": "MIT",
  "dependencies": {
    "parse5": "^8.0.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^26.1.0",
    "eslint": "^10.6.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.62.1",
    "vitest": "^4.1.9"
  }
}
